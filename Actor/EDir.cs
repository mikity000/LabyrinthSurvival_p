/**
* 方向を表す定数
*/
public enum EDir {
    Pause, // 静止(NULLの代わり)
    Up,    // 上
    RightUp, //右上
    Right, // 右
    RightDown,//右下
    Down,  // 下
    LeftDown,//左下
    Left,  // 左
    LeftUp //左上
};